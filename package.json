{
  "private": true,
  "name": "nextjs-routes-development",
  "license": "MIT",
  "author": "Tate <tatethurston@gmail.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/tatethurston/nextjs-routes"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist",
    "e2e:setup": "npm run package:build && (cd e2e && npm install)",
    "lint": "npm run typecheck && prettier --check . && prettier-package-json --list-different '{,public.,example/}package.json' && eslint .",
    "lint:fix": "prettier --write '**/*.{ts,tsx,md,json}' && prettier-package-json --write '{,public.,example/}package.json' && eslint --fix .",
    "package:build": "npm install && npm run build && npm run package:prune && npm run package:copy:files && chmod +x dist/cli.js",
    "package:copy:files": "cp ./LICENSE ./README.md dist/ && cp ./public.package.json dist/package.json",
    "package:prune": "find dist -name test.* | xargs rm -f",
    "prepare": "husky install",
    "test": "npm run e2e:setup && jest",
    "test:ci": "test --coverage",
    "typecheck": "tsc --noEmit"
  },
  "sideEffects": false,
  "devDependencies": {
    "@babel/preset-env": "^7.18.6",
    "@babel/preset-typescript": "^7.18.6",
    "@types/jest": "^28.1.4",
    "@types/node": "^18.0.3",
    "@types/react": "^18.0.15",
    "@typescript-eslint/eslint-plugin": "^5.30.5",
    "@typescript-eslint/parser": "^5.30.5",
    "babel-loader": "^8.2.5",
    "codecov": "^3.8.3",
    "eslint": "^8.19.0",
    "eslint-config-prettier": "^8.1.0",
    "husky": "^8.0.1",
    "jest": "^28.1.2",
    "next": "^12.2.0",
    "prettier": "^2.7.1",
    "prettier-package-json": "^2.6.4",
    "react": "^18.2.0",
    "typescript": "^4.7.4"
  }
}
